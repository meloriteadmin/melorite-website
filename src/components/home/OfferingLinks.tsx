import Link from "next/link";
import { businessApplications, industrySolutions } from "@/data/catalog";
import { Icon } from "@/lib/icons";
import { tint } from "@/lib/utils";
import { SectionHeading } from "@/components/shared/SectionHeading";
export function OfferingLinks() { return <><section className="section-y bg-white"><div className="container-x"><SectionHeading eyebrow="Business Applications" title={["Essential work,", "in one workspace."]} description="Seven applications that share context, data and a familiar interface." /><div className="mt-12 grid divide-x divide-y divide-line border border-line sm:grid-cols-2 lg:grid-cols-4">{businessApplications.map(app => <Link key={app.slug} href={`/business-applications/${app.slug}`} className="group p-6 hover:bg-paper"><span className="grid size-9 place-items-center rounded-lg" style={{ background: tint(app.accent,.1), color: app.accent }}><Icon name={app.icon} className="size-4" /></span><h3 className="mt-5 text-[16px] font-semibold text-navy">{app.name}</h3><p className="mt-2 text-[13px] leading-relaxed text-muted">{app.eyebrow}</p></Link>)}</div><Link href="/business-applications" className="mt-7 inline-block text-sm font-semibold text-brand">Explore all Business Applications →</Link></div></section><section className="section-y bg-[#f5f3ff]"><div className="container-x"><SectionHeading eyebrow="Industry Solutions" title={["Workflows with", "industry context."]} description="Six purpose-built solutions that run on the same connected Melorite platform." /><div className="mt-12 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{industrySolutions.map(solution => <Link key={solution.slug} href={`/industries/${solution.slug}`} className="group border border-violet-100 bg-white/80 p-6 transition hover:bg-white"><span className="text-[15px] font-semibold text-navy">{solution.name}</span><p className="mt-2 text-[13px] leading-relaxed text-muted">{solution.description}</p><span className="mt-5 inline-block text-sm font-medium text-brand">Explore →</span></Link>)}</div></div></section></>; }
